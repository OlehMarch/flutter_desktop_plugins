name: cross_connectivity
version: 1.0.0
homepage: https://github.com/marchdev-tk/cross_connectivity
description: Flutter plugin for discovering the state of the network (WiFi & mobile/cellular) connectivity on both Mobile, Web and Desktop.

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  rxdart: ^0.23.1
  connectivity: ^0.4.8+2
  connectivity_macos: ^0.1.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
